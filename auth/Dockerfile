# drasl с нашим конфигом, запечённым в образ (секретов в конфиге нет —
# ключи подписи drasl генерирует и хранит в /var/lib/drasl на томе Fly).
FROM unmojang/drasl
COPY config.toml /etc/drasl/config.toml
